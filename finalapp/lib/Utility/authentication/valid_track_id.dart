import 'package:finalapp/local_models/local_model_barrel.dart';

class IsValidID {
  checkID(id) {
    //print(idList);
    // return idList.map((e) => e.id).toList().contains(id);
    // if (result) {
    //   print('');
    // } else {
    //   print('');
    // }
  }
}
